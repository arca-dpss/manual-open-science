source("renv/activate.R")

if(interactive()){

  # Libraries
  # library("tidyverse")

  # Load custom function
  devtools::load_all()

  # Load targets
  # tar_load_all()

  # Options
  # theme_set(theme_bw())
}
